<div id="card<%=@card.id%>">
  <%= link_to(game_appearance_plays_path(game_id: @game.id, round_id: @game.this_round.id, appearance_id: @appearance.id, card_id: @card.id), method: :post, remote: true) do %>
    <h3><%= @card.name %></h3>
    <p><%= @card.bio %></p>
  <% end %>
</div>
